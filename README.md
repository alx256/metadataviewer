# Unnamed MetaData Viewer

A command-line tool that can give you information about a file or directory. This was inspired by the macOS "Get Info" functionality.

## Compilation
The tool can be compiled by running `cmake .`
